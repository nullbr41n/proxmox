# 03 Networking

Configures the MetalLB `IPAddressPool` and `L2Advertisement` after the MetalLB chart is installed.

## Usage

```bash

cd 03-networking
terraform init
terraform apply
```

Run this after `02-metallb`.
